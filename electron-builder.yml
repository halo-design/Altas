productName: Altas
appId: gh.halo-design.altas

files: 
  - app/
  - node_modules/
  - utils/
  - win/
  - package.json

compression: maximum
artifactName: ${productName}-${version}-${os}-${arch}.${ext}

dmg:
  contents:
  - x: 130
    y: 220
  - x: 410
    y: 220
    type: link
    path: /Applications
  title: Altas ${version}

mac:
  icon: win/resource/dock.icns

linux:
  icon: win/resource
  category: app.tools
  executableName: Altas
  target: 
    - deb
    - rpm
    - AppImage

win:
  target: nsis
  icon: win/resource/dock.ico

nsis: 
  oneClick: false
  installerIcon: win/resource/dock.ico
  uninstallerIcon: win/resource/dock.ico
  installerHeaderIcon: win/resource/dock.ico
  allowToChangeInstallationDirectory: true
  artifactName: ${productName}-${version}-${os}-${arch}-setup.${ext}
  deleteAppDataOnUninstall: true

directories:
  buildResources: resources
  output: packages
