{
  "name": "electron-altas",
  "version": "1.0.0",
  "description": "A Electron application",
  "main": "main.js",
  "scripts": {
    "dev": "parcel src/core/index.tsx --out-dir app/static --target electron",
    "compile": "rm -rf app/js && parcel build src/core/index.tsx --out-dir app/static --no-source-maps --target electron",
    "start": "electron .",
    "pack": "electron-packager . Altas",
    "build": "electron-packager . Altas --platform=darwin --arch=x64 --out ./dist --app-version 0.0.1 --overwrite --icon=./app/favicon.icns",
    "dmg": "electron-installer-dmg ./dist/Altas-darwin-x64/Altas.app Altas --out=mirror --icon=./app/favicon.icns --overwrite"
  },
  "repository": "https://github.com/owlaford/Altas",
  "keywords": [
    "Electron"
  ],
  "author": "OwlAford",
  "license": "MIT",
  "engines": {
    "node": ">=8"
  },
  "devDependencies": {
    "@babel/core": "^7.2.2",
    "@babel/preset-env": "^7.2.3",
    "@babel/preset-react": "^7.0.0",
    "electron": "^4.0.0",
    "electron-installer-dmg": "^2.0.0",
    "electron-packager": "^13.0.1",
    "node-sass": "^4.11.0",
    "parcel-bundler": "^1.11.0",
    "typescript": "^3.2.2"
  },
  "dependencies": {
    "react": "^16.7.0",
    "react-dom": "^16.7.0"
  }
}
